/**
 * Audio Viewer Constants
 * 
 * Shared layout and styling constants.
 */

/** Width of track header in pixels */
export const TRACK_HEADER_WIDTH = 200;

/** Default track height in pixels (slim = control panel height) */
export const DEFAULT_TRACK_HEIGHT = 64;

/** Height of each control row inside a track header (DEFAULT_TRACK_HEIGHT / 2) */
export const TRACK_HEADER_ROW_HEIGHT = DEFAULT_TRACK_HEIGHT / 2;

/** Height of the timeline ruler in pixels */
export const RULER_HEIGHT = 32;

/** Extra pixels of empty space appended past the last clip */
export const TIMELINE_END_PADDING = 200;

/** Minimum track height in pixels */
export const MIN_TRACK_HEIGHT = 60;

/** Maximum track height in pixels */
export const MAX_TRACK_HEIGHT = 300;
